# Vale (optional)

Vale is a lightweight prose linter used by many docs teams.
This repo does **not** require Vale, but adding it can demonstrate docs-as-code maturity.

If you enable Vale, keep rules minimal:
- consistency (terminology)
- avoid vague “easy/simple”
- heading punctuation

See `vale.ini` for configuration and `styles/` for rules.
